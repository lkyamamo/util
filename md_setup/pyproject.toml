[project]
name = "md-setup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "pandas>=2.0",
    "pymatgen>=2024.1.1",  # LammpsData, used by lammps_data.py and hand_place/
    "ase>=3.23",           # both structure formats, used by structure_io.py and water_path/
]
