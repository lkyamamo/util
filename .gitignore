*POTCAR*
calculations/lammps/dumps/*
*__pycache__*
*.venv
*.claude
jobs/pipeline/lammps_to_analysis/submit_pipeline.conf
jobs/pipeline/lammps_to_temperature_sweep/submit_temperature_sweep.conf
NAS/pipeline/manifests/
NAS/pipeline/logs/

# Numeric dumps and run logs: generated output, regenerated by whatever wrote
# them. The .dat files already tracked stay tracked - ignoring never applies to
# a file git is already following - so this only stops new ones appearing.
*.dat
*.log
